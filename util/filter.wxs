var format =  function(text){
  // 数据初始化为undefined
  if(!text){
    return 
  }
  var reg = getRegExp("\\\\n", "g")
  return text.replace(reg,"\n&nbsp;&nbsp;")
  
}
module.exports = {
  format : format
}